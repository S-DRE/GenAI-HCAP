# Python 3.12 required — the AI/ML ecosystem (LangChain, ChromaDB, etc.)
# does not yet support Python 3.13+.
# To create the venv: py -3.12 -m venv .venv  (Windows, Python Install Manager)
#                     python3.12 -m venv .venv  (Mac/Linux)

# Core LLM & Agent
langgraph==1.0.10
langchain==1.2.10
langchain-groq==1.1.2
langchain-community==0.4.1
langchain-core==1.2.17
langsmith==0.7.13

# Vector Store & Embeddings
chromadb==1.5.2
sentence-transformers==5.2.3
langchain-huggingface==1.2.1
langchain-chroma==1.1.0

# API
fastapi==0.135.1
uvicorn[standard]==0.41.0
python-multipart==0.0.22

# Voice (local, no API cost)
# Install separately when needed — large model downloads (~1-2 GB):
#   .venv\Scripts\pip install openai-whisper TTS
openai-whisper
TTS

# Safety & Guardrails (manual keyword rules in src/guardrails/validators.py)

# Configuration & Utilities
python-dotenv==1.2.2
structlog==25.5.0
pydantic==2.12.5

# Testing
pytest==9.0.2
pytest-cov==7.0.0
pytest-asyncio==1.3.0
httpx==0.28.1
